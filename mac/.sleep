#!/bin/bash
#
# You can invoke a file
# bash ~/file
#
# But you cannot use variables (i dont know why)
# and you cannot use outside functions only inside like below

function runit() {
  echo "sleep!" > ~/Desktop/sleepalert.txt
}
#runit

#osascript -e 'tell application "Notes" to quit'

# Turn off wifi
networksetup -setairportpower en0 off


